# Download and run

```bash
mkdir PyNimal && cd PyNimal && pkg update && pkg upgrade && pkg install wget python && wget https://github.com/lubro0/PyNimal/releases/download/newest/installer.py && python installer.py
